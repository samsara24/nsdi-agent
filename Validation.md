# nsdi-agent 待确认事项台账

本文记录**无法在本仓库内自行闭环**的待确认事项。它与 `Progress.md` 的分工是：

- `Progress.md` 记录已经做完的事和已经测出来的数字。
- `Validation.md` 记录还需要人来拍板、需要外部输入、或需要新数据才能验证的事。

写入规则：

1. 每条必须写清楚**现有证据**、**阻塞了什么**、**未确认前的默认取值**。
2. 不写「待讨论」这种没有默认值的条目。任何一条在没被确认之前，代码里都必须有一个
   可运行的默认行为，并且这个默认行为要在这里写明。
3. 确认后把状态改为 `已确认` 并写上结论与日期，不要删除原条目。
4. 涉及代码常量的条目，确认后必须同步改代码里的版本号或 `review_status`，否则视为未确认。

状态图例：`待确认` / `已确认` / `已否决` / `已过期`。

责任人缩写：夏（夏思博）、黄（黄泽舜）、王（王雅琪）、谢（谢其桐）、陈（陈子昂 / 本人）。

### V25 远端模型路径与实际 GPU 拓扑

| 项 | 内容 |
| --- | --- |
| 状态 | `已确认`（2026-08-17） |
| 责任人 | 陈 |
| 阻塞 | 无 |
| 提出于 | Expanded 343-case 远端实验，2026-08-17 |

用户确认模型为 `/home/chenziang/pretrained_models/DeepSeek-R1-Distill-Qwen-32B`，与仓库历史
正式实验记录一致。默认恢复为 BF16、TP=2、`gpu_memory_utilization=0.85`、
`max_model_len=8192`、`max_new_tokens=512`，两张卡各要求至少 44000 MiB 空闲；正式运行前仍用
`NSDI_RCA_DRY_RUN=1` 检查当前环境与 GPU 状态。

---

## 1. 阻塞主线的决策项

这一类不确认就无法继续往下做，或者继续做出来的结果不可用。

### V0 全覆盖多维置信度默认参数

| 项 | 内容 |
| --- | --- |
| 状态 | `待确认` |
| 责任人 | 陈 + 夏 |
| 阻塞 | N6 置信度阈值定稿、论文实验表的 coverage / accuracy 工作点 |
| 提出于 | Loop2，2026-08-13 |

现有证据：`docs/个人整体思路.md` 要求大模型综合证据完整度、物理约束合规性、推导完备性、历史案例相似度输出置信度阈值。Loop1 选择性口径下 M9 最终自动结论为 0，无法建立置信度与准确率的关系；因此本轮改为每个 test case 都产出 L1/L2/fiber 候选和四维置信度，再由阈值决定自动结案还是降级。

未确认前的默认取值：

1. 四维综合权重为 `evidence_completeness=0.30`、`physical_compliance=0.30`、`reasoning_completeness=0.20`、`history_similarity=0.20`。
2. N6 默认自动结案阈值沿用 `0.50`，正式采用前以 `summary.json.threshold_sweep` 重新选择。
3. 旧 veto 不再丢弃结论，而是折减 `physical_compliance`：blackout 与 fiber 可识别性 veto 上限 `0.30`，群体先验作证据上限 `0.40`，其他量测契约 veto 上限 `0.50`。

这些默认值只用于生成标定曲线，不得单独拿全覆盖 accuracy 宣称方法有效。

### V1 N4 分流阈值必须重新标定

| 项 | 内容 |
| --- | --- |
| 状态 | `待确认`（T4 已产出标定曲线，给出明确建议，等拍板） |
| 责任人 | 陈 + 夏 |
| 阻塞 | T5 三分支路由；没有阈值就没有 N5a / N5b / N5c 的边界 |
| 提出于 | T1，2026-08-07；T4 补充标定证据，2026-08-07 |

背景：画板定稿的 `sim = 100%` / `70% <= sim < 100%` / `sim < 70%` 是在 legacy
`anomaly_id` 特征空间上定的。换到特征字典 v1 之后完全匹配从 46/85 掉到 21/85，
训练集 126 条里 104 条（82.54%）是唯一 signature。

T4 已用训练集留一法产出 coverage-accuracy 曲线（`artifacts/t4_threshold_calibration.json`，
分析见 `Progress.md` 9.14），四条结论：

1. **`sim = 1.0` 保留。** 累计曲线看起来 0.90 更优，但 `[0.9, 1.0)` 区间训练集只有 2 条、
   测试集 0 条，不足以支撑改动。
2. **`sim >= 0.7` 应当废弃。** 在 `sim = 1.0` 之下、`sim > 0` 之上的所有分档，
   准确率都在 60-70% 之间摆动，与 65.87% 的类别先验没有区别，两个切分上排序还不一致。
   相似度一旦低于 1.0，它的具体数值就不再携带准确率信息，拿它切边界切出来的是噪声。
3. **N5b 改用「证据全覆盖」（`evidence_coverage = 1.0`）。** 含义是「历史上存在一个 case
   包含了我的全部证据」。这是唯一在两个切分上都能复现的分档信号
   （train-LOO 73.77% / held-out 79.49%，均显著高于先验）。
4. **`sim = 0` 直接进 N6。** 训练集 2 条零证据 case 留一法准确率 0/2。

建议采纳的 N4 v2：`tokens` 为空 → N6；`sim = 1.0` → N5a；`evidence_coverage = 1.0` → N5b；
其余 → N5c。头对头对比下，高置信出口准确率 train-LOO 71.64% → 73.77%、
held-out 68.09% → 79.49%，代价是覆盖率降 5-9 个百分点。

需要夏拍板的只剩一件事：**是否接受用「证据全覆盖」替换画板上的 70% 这个数字**。
这会改变画板与论文里 N4 的表述，从「相似度分三档」变成「先看有没有完全一样的历史 case，
再看历史有没有见过我全部的证据」。后者其实更好讲，但它不再是一个可调的阈值参数，
如果论文需要一条「阈值敏感性」曲线，就得另外设计。

需要注意：held-out 上的提升幅度（+11.4 个百分点）远大于 train-LOO 上的（+2.1），
样本量只有 39-67 条，因此**提升的方向可信，幅度不可信**。合并数据集到位后必须重验（见 V10）。

未确认前的默认取值：**T5 的路由器把两套规则都实现，默认走 v2，
用一个配置项可以切回画板的 100% / 70%**。两套都有测试锁定当前数字，
这样拍板往哪边倒都不需要返工。

### V2 是否接受「N5a 变小」这个代价

| 项 | 内容 |
| --- | --- |
| 状态 | `待确认` |
| 责任人 | 夏 |
| 阻塞 | 论文里 N5a 分支的叙事写法 |
| 提出于 | T1，2026-08-07 |

现有证据：T1 的两条验收指标本身是冲突的。压低混合标签覆盖率必然推高 signature 唯一性，
从而压缩完全匹配桶。1023 个子集的消融显示，这不是某个特征选得不好，而是整个权衡面的形状。

具体数字：legacy 下 N5a 46 条、桶内多数投票 60.87%；v1 下 N5a 21 条、桶内多数投票 76.19%。
换句话说，v1 用「少一半的完全匹配」换来「高 15 个点的桶内准确率」。

需要确认的是论文口径怎么写：是把 N5a 定位成「高置信度小流量出口」，
还是要求它必须覆盖相当比例的 case。前者与当前实现一致，后者需要回头放宽特征粒度。

未确认前的默认取值：**按「高置信度小流量出口」写**，并在 N6 让未落入 N5a 的 case
走 N5b / N5c，而不是硬塞进 N5a。

### V3 fiber 的目标指标是什么

| 项 | 内容 |
| --- | --- |
| 状态 | `待确认` |
| 责任人 | 夏 |
| 阻塞 | T10 实验设计、T12 论文结论口径 |
| 提出于 | T1，2026-08-07 |

现有证据：v1 把 `fiber` F1 从 0 提到 0.20（precision 0.25、recall 0.1667，即 6 条测试
case 判对 1 条）。此前 deterministic 58/85、DeepSeek-32B vLLM 59/85、
全特征 RandomForest 5 折三条基线的 fiber precision / recall / F1 全部是 0。

问题是：0.20 算不算一个可以写进论文的结果。当前数据集 fiber 只有 14 条有效 case，
60/40 切分后训练 8 条、测试 6 条。在 6 条测试样本上，判对 1 条和判对 2 条之间
F1 差 0.15，统计上没有区分度。

未确认前的默认取值：**只把「首次非 0」作为事实陈述，不作为方法有效性的证据**，
并在所有报告里同时给出 fiber 的绝对样本量。等合并数据集（见 V10）后重测。

---

## 2. 需要领域专家确认的技术项

### V4 物理约束库 14 条待逐条审核

| 项 | 内容 |
| --- | --- |
| 状态 | `待确认` |
| 责任人 | 夏 |
| 阻塞 | T6 约束校验器；未审约束不能作为 LLM 推理的硬依据 |
| 提出于 | T2，2026-08-07 |

现有证据：`rca_framework/constraints/library.py` 已有 14 条，
版本 `constraint-library-v1`，`content_hash=ee95eddd7885abdf`。
可读版本在 `skills/rca-constraints/SKILL.md`，由代码自动渲染。

14 条的 `review_status` 全部是 `pending_expert_review`。渲染进 prompt 时会带
「（待专家审核）」后缀，这一点有测试锁定，防止未审约束被当成已确认事实。

审核时请重点看这几条，它们的参数来自本数据集实测而不是器件规范，最可能有偏：

| ID | 参数 | 实测依据 | 想确认什么 |
| --- | --- | --- | --- |
| `C2_bias_healthy_band` | 偏置电流 7.2-7.8 mA | 训练集非零电流 p25=7.22/7.29，p99=7.72/7.78 mA | 这是模块规范值还是本批次的巧合 |
| `C5_tx_power_range` | 发送功率 -1.8~2.1 dBm | 963 个健康读数的实际范围 | 规范的发送功率窗口是多少 |
| `C7_rx_power_range` | 接收功率 -12.3~3.0 dBm | 929 个健康读数的实际范围 | 接收灵敏度下限的规范值是多少 |
| `C11_media_snr_floor` | 介质侧 SNR 下界 22.5 dB | 健康值 p1 = 22.47 / 22.95 dB | 22.5 dB 是不是有效的告警门限 |

确认后请把对应条目的 `review_status` 改为 `approved`，并把 `provenance` 从
`measured` 改成 `device_spec`（如果给出的是规范值）。改完要重新跑
`python scripts/render_constraint_skill.py`。

未确认前的默认取值：**照现状注入 prompt，但带「待专家审核」标记**。

### V5 L2 侧温度系统性高于 L1 侧约 3.5 °C 的原因

| 项 | 内容 |
| --- | --- |
| 状态 | `待确认` |
| 责任人 | 夏 |
| 阻塞 | `C3_temperature_operating_range` 的措辞；影响 N5c prompt 会不会把温差当证据 |
| 提出于 | T2，2026-08-07 |

现有证据：训练集 126 条中，L1 侧温度中位数 46.72 °C，L2 侧 50.30 °C，
差 3.58 °C；两侧全部落在 39.35-60.13 °C，无一超出 0-70 °C 工作范围。

当前约束里写的是「这是 200G 与 400G 端口的形态差异，不是故障」，这是推测。
需要确认：是端口形态差异、散热位置差异、还是采集口径差异。

未确认前的默认取值：**按形态差异处理**，即禁止把两侧温差当作差异证据。
这个默认是保守的：即使推测错了，也只是少用了一个弱信号，不会产生错误结论。

### V6 单例低电压读数是否真实

| 项 | 内容 |
| --- | --- |
| 状态 | `待确认` |
| 责任人 | 夏 + 黄 |
| 阻塞 | `C4_voltage_nominal_band` 能否作为排除条件使用 |
| 提出于 | T2，2026-08-07 |

现有证据：训练集 252 个电压读数中，只有 `case_aa307cc7c7db` 的 L2 侧一例为 3.10 V，
低于 3.3 V ±5% 的下界 3.135 V。其余全部落在 3.14-3.42 V。

需要确认：这一例是真实的供电异常，还是采集抖动 / 单点坏值。
如果是真实异常，它应该与该 case 的标签一致；如果是坏值，约束的排除方向要反过来写。

未确认前的默认取值：**按真实异常处理，但只作为人工确认触发条件，不建统计规则**，
因为 1/252 的样本量本来也支撑不了规则。

---

## 3. 需要数据方 / 厂商确认的量测项

这一类是数据质量问题，不是算法问题，在本仓库内无法闭环。

### V7 两端 lane 编号是否物理对应

| 项 | 内容 |
| --- | --- |
| 状态 | `待确认` |
| 责任人 | 黄 + 王（需向设备厂商确认） |
| 阻塞 | 所有跨端点的方向性判断；`C12` 是否可以撤销 |
| 提出于 | T1 / T2，2026-08-07 |

现有证据（这是本次重构发现的最硬的一条数据质量问题）：
把 L1 侧发送功率与 L2 侧接收功率按 lane 号配对相减后，
`L1_to_L2` 方向的均值损耗中位数是 **-0.285 dB**，`L2_to_L1` 是 **-0.227 dB**。
两个方向的中位数都是负值，意思是「对端收到的光比本端发出的还多」。
无源链路上这在物理上不可能。

三个可能的原因，需要确认是哪一个：

1. 两端 lane 编号不对应。L1 是 400G、L2 是 200G，lane 号未必指向同一根纤芯。
2. 收发功率的标定口径不同（例如一端是模块口功率，另一端是芯片口功率）。
3. 采集时刻不同步，两端读数不是同一时刻的。

影响范围：legacy `anomaly.fit_thresholds` 学到的 `loss_upper` 是
`L1_to_L2: 3.10875 dB` / `L2_to_L1: 3.42 dB`，这两个数建立在上述有问题的相减之上，
因此**不可信**。当前已通过 `C12_no_absolute_link_loss` 禁止使用绝对损耗门限。

未确认前的默认取值：**禁止任何绝对链路损耗推断**，只用同侧内部的相对量
（lane 间极差）和有光 / 无光判断。这已经在约束库和特征字典 v1 里落实。

### V8 `serdes_snr` 字段的量纲

| 项 | 内容 |
| --- | --- |
| 状态 | `待确认` |
| 责任人 | 黄 + 王（需向设备厂商确认） |
| 阻塞 | legacy 符号规则的可解释性；`C13` 是否可以撤销 |
| 提出于 | T2，2026-08-07 |

现有证据：训练集 972 个 `serdes_snr` 读数，健康值 p25-p99 在 6.6e5-8.2e5 量级，
失效时为 1。这不是任何 dB 口径的信噪比。

影响范围不小：legacy 符号规则里出现频率最高的项之一就是
`low_outlier:L1:serdes_snr` 和 `low_outlier:L2:serdes_snr`，它们建立在一个
量纲未知的字段上。也就是说 legacy 的部分「可解释规则」其实解释不了。

未确认前的默认取值：**只当作有效 / 失效的二值信号**，不允许出现
「serdes SNR 低了 x dB」这类表述。特征字典 v1 的 `level_tail` 家族
没有把 `serdes_snr` 纳入连续量分档，正是出于这个原因。

### V9 `host_snr` 大面积缺失的原因

| 项 | 内容 |
| --- | --- |
| 状态 | `待确认` |
| 责任人 | 黄 |
| 阻塞 | N6 的证据充分性判定 |
| 提出于 | T2，2026-08-07 |

现有证据：126 条训练 case 中只有 34 条任一侧有 `host_snr` 读数，缺失率 73%。

需要确认：是采集脚本没取、是部分型号不支持、还是这个字段本来就是可选的。
这决定了「缺失」在 N6 里应该算「未采集」还是「不适用」——前者要触发补采请求，
后者不该触发。

未确认前的默认取值：**按「未采集」处理**（`C14_host_snr_mostly_missing`），
即缺失不等于正常，也不允许用它支持任何结论。

### V10 合并数据集的交付时间与 fiber 有效样本数

| 项 | 内容 |
| --- | --- |
| 状态 | `待确认` |
| 责任人 | 黄 + 王 |
| 阻塞 | T1 选型复核、T5 阈值标定、T10 全部消融、V2 / V3 的最终结论 |
| 提出于 | 排期，2026-08-07 |

现有证据：当前 `organized_data` 只有 211 条有效 case，其中 fiber 14 条
（训练 8 / 测试 6）。N5b 在 legacy 口径下只有 8 条，v1 口径下 26 条。

需要确认：合并后的总量、fiber 的绝对条数、是否需要重新做分层切分。

影响的具体动作（数据到位后必须重跑，不是可选项）：

1. 重跑 `scripts/sweep_feature_families.py`，用同一套 train-LOO 规则复核 v1 家族选型。
   特别是 `lane_direction` 家族——它在当前数据集上落选，但落选理由是「没有增量」，
   不是「物理上无效」，样本量变化后结论可能反转。
2. 重新拟合 `FeatureModel` 的分位边界，当前边界绑定 126 条训练 case。
3. 重新标定约束库里 7 条 `measured` 类参数。
4. 重新标定 N4 阈值（V1）。

未确认前的默认取值：**继续用现有 211 条切分推进代码，所有结论标注数据集版本**。

### V11 专家排障 SOP（M6）是否按期到位

| 项 | 内容 |
| --- | --- |
| 状态 | `待确认` |
| 责任人 | 黄 + 谢 |
| 阻塞 | T6 的 N5c prompt；SOP 是 N5c 除约束库之外的另一半输入 |
| 提出于 | 排期，2026-08-07 |

排期要求 SOP 在 Day 8 前进入 M6，否则用兜底 SOP。

未确认前的默认取值：**用兜底 SOP**，内容是「按 C1-C14 的排除条件顺序逐项检查」，
即把约束库的执行顺序直接当作 SOP。这个兜底已经可用，不阻塞 T6 开工。

---

## 4. 数据集本身的可疑观测

这一类不阻塞开发，但会影响结论的可推广性，需要在论文里如实说明。

### V12 告警端口恒为 L2 侧

| 项 | 内容 |
| --- | --- |
| 状态 | `待确认` |
| 责任人 | 黄 |
| 阻塞 | 不阻塞；影响论文的适用范围声明 |
| 提出于 | T1，2026-08-07 |

现有证据：全部 211 条 case 的 `alarm_ip_interface` 都指向 L2 侧（200G 端口），
无一例外。也就是说这个字段的信息量为零。

需要确认：是采集时只订阅了 200G 侧的告警，还是 400G 侧确实不产生这类告警。
如果是前者，说明数据集有系统性的采集偏差，L1 侧的告警信息完全缺失。

未确认前的默认取值：**不把该字段作为特征**（特征字典 v1 未纳入），
并在论文的数据集描述里如实写明这一点。

### V13 告警类型只有 3 种

| 项 | 内容 |
| --- | --- |
| 状态 | `待确认` |
| 责任人 | 黄 |
| 阻塞 | 不阻塞；影响 `alarm_kind` 家族的价值判断 |
| 提出于 | T1，2026-08-07 |

现有证据：211 条 case 的 `alarm_name` 只有三个取值：
「DCN-AI超平面-接口降lane-网络侧」162 条、「DCN-AI超平面-频繁升降lane-网络侧」46 条、
「DCN linkflap告警」3 条。三者的标签分布都接近全局先验。

需要确认：是数据集只筛了这三类告警，还是现网就只有这三类。
这决定了 `alarm_kind` 家族在更大数据集上还有没有价值。

未确认前的默认取值：**保留为 `candidate` 家族，不进 v1 signature**。

---

## 5. 口径与记录类

### V16 全链路遥测失效的 6 条 case 该不该回答

| 项 | 内容 |
| --- | --- |
| 状态 | `已确认`（2026-08-17：expanded expert-clean v1 剔除全部 6 条） |
| 责任人 | 夏 + 黄 |
| 阻塞 | 不阻塞（已实现为可配置开关），但影响 N6 的边界定义与论文口径 |
| 提出于 | T5，2026-08-07 |

这是 T5 跑「确定性物理排除有没有排掉过真实标签」这项全量校验时挖出来的，
是本次重构目前发现的第二硬的数据质量问题（第一是 V7 的负损耗）。

现有证据：全量 211 条中有 6 条，两侧 4 个 lane 的 `txpower` 与 `rxpower` **全部**是
-40.0 dBm，介质侧信噪比也全部触底，而状态位一律是
`TxLOS=Normal`、`TxLOL=Normal`、`RxLOS=Abnormal`、`RxLOL=Abnormal`。

`TxLOS=Normal` 与 `txpower=-40` 直接矛盾：模块若真的关断了激光，TxLOS 应当告警。
更合理的解释是链路整体中断后遥测通道本身失效，所有光学读数回落到哨兵默认值——
**此时哨兵表示「读不到」而不是「没有光」**。

这 6 条的标签是 L2 四条、fiber 两条。也就是说物理观测完全一致而根因不同。
它们在特征空间里之所以还能分出 4 个 signature，唯一的区别来自 `serdes_snr`
是 `partial_lanes` 还是 `all_lanes`——而 `serdes_snr` 的量纲我们并不知道（见 V8）。

影响与已做的处置：

1. 这直接证伪了 `C6`（本端未发光则排除介质根因）的一个前提。加前置条件之前，
   `C6` 在全量上触发 14 次、其中 2 次排掉了真实的 fiber 标签。
2. 已新增约束 `C15_blackout_sentinel_is_not_laser_off` 作为 `C6` 的前置条件，
   约束库升到 `constraint-library-v2`（15 条，`content_hash=abb395e9371abc36`）。
   加上前置条件后 `C6` 触发 8 次、排错 0 次。
3. 已给路由器加 `abstain_on_optical_blackout` 开关，`coverage-v2` 默认打开，
   把这 6 条送 N6 转人工。

需要拍板的是第 3 点该不该保留，因为**实测数据并不支持它**：

| 处置 | 留出测试集给结论数 | 给结论时准确率 |
| --- | ---: | ---: |
| 回答（关闭开关） | 39 | 31/39 = 79.49% |
| 弃权（打开开关，当前默认） | 37 | 29/37 = 78.38% |

在这 6 条上按历史多数投票回答能对 5 条（83.33%），高于 65.40% 的全局多数类先验。
所以弃权是**净损失**，代价是每 85 条少答对约 2 条。

支持仍然弃权的理由只有一条，但我认为它足够：
这 5/6 的正确率完全建立在 `serdes_snr` 上，而我们已经在 `C13` 里正式声明该字段
量纲未知、只能作二值使用。用一个我们承认读不懂的字段去区分根因，
然后对外报告 83% 的置信度，这在论文里是站不住的。而且 n=6，
5/6 与先验的差异在统计上不显著。

确认结论：用户选择把这 6 条从 `expanded-expert-clean-v1` 的 train/test 契约中全部剔除，
而不是让分类器回答或仅降级。legacy 数据与既有基线不删除，仍保留作回归锚点。

另需向黄确认的量测问题：**链路整体中断时，遥测通道是否会把所有光学读数写成哨兵默认值？**
如果是，那么整个数据集里所有的「断光」证据都需要重新审视一遍——
我们无法区分哪些是真的没有光，哪些只是没读到数。这个问题的影响面比这 6 条大得多。

### V17 T6 真实模型首测

| 项 | 内容 |
| --- | --- |
| 状态 | `已确认`（2026-08-07 已完成 DeepSeek-R1-Distill-Qwen-32B/vLLM 首测；2026-08-09 补记） |
| 责任人 | 陈 + 黄 |
| 阻塞 | GPU 资源不再阻塞 T6；正式可复现实验、消融和方法有效性结论仍未完成 |
| 提出于 | T6，2026-08-07 |

确认结果：已使用 `/home/chenziang/pretrained_models/DeepSeek-R1-Distill-Qwen-32B`
通过 vLLM 0.11.0、tensor parallel 2 完成冒烟和 85 条留出集首测。
产物为 `artifacts/t6_smoke_promptv2.json/.log` 与
`artifacts/t6_llm_evaluation.json/.log`。

`coverage-v2` 下 N5c 共 46 条：模型给结论 22 条、判对 5 条（22.73%），
主动弃权 24 条；全链路回答 59/85、答对 34 条，给结论时准确率 57.63%，
全量准确率 40.00%，没有超过 legacy 58/85。

首测关闭了“是否有 GPU、模型会不会弃权”两个问题，但新暴露出：

1. 聚合产物没有单独报告首轮与重写通过率。
2. checker 能保证引用结构合法，却不能保证约束适用于被引用证据；
   真机输出已有物理语义错配仍通过校验的 badcase。
3. 结果文件缺正式 `run_manifest`、逐 case outcome/trace、类别指标和 prompt hash，
   因此只能作为工程首测，不能作为论文正式实验。

后续默认取值仍是：无模型环境使用 **`--llm-backend none`** 作为“不接 LLM”对照；
正式实验沿用已验证的 32B 模型配置，但必须先补齐上述复现门禁。

### V18 M9 最终出口的 Wilson 下界与最小支持数

| 项 | 内容 |
| --- | --- |
| 状态 | `待确认`（已实现为可配置参数，不阻塞编码） |
| 责任人 | 陈 + 夏 |
| 阻塞 | T10 置信度阈值消融与论文中的最终 coverage / risk 工作点 |
| 提出于 | T7，2026-08-10 |

现有证据：`coverage-v2` 的训练集留一法中，`N5a_pure` 为 10/12、Wilson 95% 下界
55.20%；`N5b_minor_gap` 为 26/37、下界 54.22%。`N5a_mixed` 与
`N5b_critical_gap` 的下界只有 30.00% / 30.06%，样本支持数分别为 6 / 4。
因此按下界而不是点估计做门禁，可以把两个小样本高抖动组降级，同时保留纯匹配和
非关键缺失两类已有统计支持的出口。

需要确认的是论文最终工作点应采用多高的下界、最小支持数是否应随数据规模变化。
当前 211 条数据不足以把这两个数冻结为领域常量。

未确认前的默认取值：**Wilson 95% 下界至少 0.50，标定支持数至少 10**。
两者分别由 `evaluate_routing.py --decision-lower-bound` 和
`--decision-min-support` 配置；T10 必须报告阈值曲线，不能只报告这一个默认点。
未独立标定的 LLM 自报 confidence 即使很高，下界也固定为 0，不允许直接进入最终报告。

### V19 l2fixed learned SOP 的论文口径与风险

| 项 | 内容 |
| --- | --- |
| 状态 | `已确认`（用户选择标签决策树路线，2026-08-10） |
| 责任人 | 陈 + 夏 |
| 阻塞 | 论文中 SOP 的命名和实验解释 |
| 提出于 | RCA v2 重构，2026-08-10 |

背景：`rca_v2_l2fixed` 只有最终 `L1` / `L2` / `fiber` 标签，没有“先检查什么、缺什么证据、
现场做了什么动作”的过程标注。因此无法从训练集直接恢复专家 SOP。用户已明确选择
“直接从标签与特征学习决策树，接受过拟合和 fiber 小样本风险”。

已采用的默认实现：`rca_framework/sop/` 中的 `learned-sop-v1` 是浅层可解释决策树，
只用 manifest train split 学习。每个叶子必须记录路径条件、标签分布、支持数、点估计
和 Wilson 下界；M9 仍按下界与最小支持数决定 final / request_evidence / human_review。

必须坚持的论文口径：

1. 它叫 **learned SOP** 或 **data-derived SOP**，不能写成专家 SOP。
2. test split 只做最终评估，不能根据 test badcase 回改树结构或阈值。
3. fiber train 只有 12 条，任何 fiber 相关叶子都必须报告绝对样本数。
4. 若后续拿到专家 SOP，应作为独立 `expert-sop-v*` 与 learned SOP 做消融对比。

未确认前的默认取值：继续使用 `learned-sop-v1` 作为 v2 dry-run 的 SOP 输入，但所有报告
必须展示叶子支持数与 Wilson 下界，不允许只展示树给出的类别。

### V14 N5a 并列 case 多数投票的口径修正

| 项 | 内容 |
| --- | --- |
| 状态 | `已确认`（陈，2026-08-07） |
| 责任人 | 陈 |

背景：`Progress.md` 第 6 节原先记录 legacy 的「N5a 并列 case 多数投票 29/46 = 63.04%」。
T1 用统一并列打破规则（按 `ROOT_CAUSES` 顺序取最小）重算后是 28/46 = 60.87%，
Top-1 仍是 29/46 = 63.04%。

结论：后续一律以 `scripts/analyze_signature_resolution.py` 的口径为准，
`Progress.md` 第 6 节与第 9.5 节已同步修正并写明差异来源。

### V15 legacy 全测试集匹配准确率两个数字的差异

| 项 | 内容 |
| --- | --- |
| 状态 | `已确认`（陈，2026-08-07） |
| 责任人 | 陈 |

背景：第 9.5 节记录 legacy 纯匹配准确率是 39/85 = 45.88%，
而第 6 节记录的是 N5a 桶内的 29/46 = 63.04%，两者看起来矛盾。

结论：不矛盾，是统计范围不同。45.88% 是**全部 85 条**上的纯历史匹配多数投票，
其中 22 条在训练集里找不到任何非零相似度的邻居，计为未命中；
63.04% 只统计落在 N5a 的那 46 条。两个数字都对，引用时必须写清范围。

### V20 非自进化正式实验采用 SOP+LLM 与训练知识包边界

| 项 | 内容 |
| --- | --- |
| 状态 | `已确认`（用户，2026-08-11） |
| 责任人 | 陈 |
| 阻塞 | 无；正式实验受 GPU、全量测试和资源释放门禁约束 |

用户已确认本轮采用 **SOP+LLM**，并明确暂不考虑自进化/演进。正式实验的固定边界为：

1. 只用 `rca_v2_l2fixed` manifest train split 构建阈值、FeatureModel、历史向量、
   EvidenceGraph、learned SOP、分支/LLM 标定和训练诊断链。
2. 训练知识必须先持久化为带 hash 的 bundle，再从磁盘重新加载；test 侧只读使用，
   不得依据 test badcase 修改知识。
3. learned SOP 是训练数据归纳的统计检查路径，作为 LLM 先验，但不是当前 case 物理证据；
   正式结论必须继续经过约束校验和 M9。
4. 实验前检查 GPU，结束后显式释放 vLLM/CUDA 资源并从进程外复核。
5. 产出总览 HTML 和逐 case HTML，逐条保存证据、SOP、LLM trace、约束违规和降级原因。

本确认关闭了“SOP-only 还是 SOP+LLM”的架构分歧，但不改变 V4/V7/V8/V16：
约束仍待专家审核，lane 对应、serdes 量纲和 blackout 语义仍是外部风险。

2026-08-11 正式实验已按该边界完成，产物为
`artifacts/offline_sop_llm_l2fixed_deepseek32b_seed42_promptv6/`。GPU 检查、知识包重载、
107 个逐 case HTML、资源释放均通过；效果侧 M9 最终自动覆盖率为 0，N5c 候选仅 1/10 正确，
SOP 对 fiber 为 0/8。V20 的工程与实验执行已关闭，但这些效果缺口继续受
V4/V7/V8/V10/V18 约束，不能把“实验跑通”解释为“方法有效”。

### V21 专家决策树 fiber 分支条件是否放宽

| 项 | 内容 |
| --- | --- |
| 状态 | `待确认` |
| 责任人 | 陈 + 夏 |
| 阻塞 | 不阻塞（现按 `docs/EXPERT_EXPERIENCE.md` 原样实现），但决定专家规则能否给出任何 fiber 结论 |
| 提出于 | 专家规则校验，2026-08-12 |

`docs/EXPERT_EXPERIENCE.md` 的 fiber 只有一个入口：两端都有有效结果、两端 priority
字符串完全相等、且两端定界不同。在 `organized_data` 231 例上按该条件实现后，
**该分支 0 次触发**，fiber recall 为 0。

原因是分解出来的，不是实现问题：231 例中 157 例只有单端产出结果，48 例两端都没有异常，
真正两端都有结果的只有 16 例，而这 16 例的 priority 全部不相等，于是一律走
「取高优先级」而非 fiber。priority 由「规则序号 + 异常级别」拼成，两端要恰好相等本身就很罕见。

未确认前代码采用的默认取值：完全按文档原条件实现，不做放宽，fiber 记为 0 召回。

若要让专家规则具备 fiber 判定能力，需要夏思博确认放宽方向，候选包括：改为比较故障模式
优先级（十位）而非完整 priority；或引入双向对称损耗证据作为独立 fiber 入口。
这与 V3（fiber 的目标指标）应一并拍板。

### V22 `all_data` 是否可正式认定为旧 organized 数据集的严格扩充版

| 项 | 内容 |
| --- | --- |
| 状态 | `待确认` |
| 责任人 | 黄 + 王 + 陈 |
| 阻塞 | 扩充实验的数据契约命名、正式新增样本数量、论文中的数据来源说明 |
| 提出于 | 扩充模式冲突实验，2026-08-16 |

现有证据：旧 manifest 有 211 条有效 case，新 manifest 有 463 条，但两套脱敏命名空间不同，
`case_id` 与 `source_hash` 的交集都为 0，不能按编号去重。剔除脱敏身份、位置、时间等字段并
规范化 `数通设备syslog告警:` 前缀后，以“物理遥测 + 告警语义 + label”的 SHA-256 内容指纹
核对，只能确认 205 条旧 case 在新集中仍存在；另有 6 条旧 case 找不到对应项。因此新集相对
旧集的可确认新增 case 是 258 条，而不是用 463 - 211 得到的 252 条。

需要数据方确认：这 6 条是被质量清洗主动删除、源数据发生修订，还是新数据打包遗漏；同时提供
`archive/all_data_source_manifest_seed42.json` 或原始相对路径映射，才能从源文件层最终确认。

未确认前的默认取值：**按保守内容集合差使用 258 条新增 case，旧 126 train 不变，旧 85 test
不变，expanded test 为 343 条；报告显著标注“新集不是已证实的严格超集”**。不把 6 条缺失旧
case 自动映射到低相似的新 case，也不依据测试结果修改标签、特征、阈值或证据图。

2026-08-17 补充：用户已确认在新的 `expanded-expert-clean-v1` 契约中删除这 6 条，因此该版本
使用 train 122 / test 341；但这不等于数据方已经确认 `all_data` 是严格扩充版，V22 的来源
追溯问题仍保持待确认。

### V23 可观测证据子图相似度的 schema 与阈值

| 项 | 内容 |
| --- | --- |
| 状态 | `待确认` |
| 责任人 | 陈 + 夏 |
| 阻塞 | 只阻塞将第二维接入正式 N4 路由；不阻塞离线 HTML 描述性审计 |
| 提出于 | 扩充模式冲突二维口径修正，2026-08-16 |

现有证据：旧 126 条训练 case 的 `CaseDiagnosis` 人工确认路径为 0 条，
因此当前无法计算真正的归因/SOP 路径相似度。新 HTML 把离散物理契约和旧 126 train
监督学习的稳定连续范围投影成 `side -> measurement -> predicate -> symptom -> physical-layer`
五层可观测子图；图中不增加候选根因节点。Q25/Q75 与 3×IQR 仍可留在特征维度，
但不再作为图中的连续谓词边界。
该子图是带类型关系的观测图，不具备有序判定、互斥分支和根因叶子，**不是决策树**；
learned SOP / 专家规则树必须作为独立对象建模，不能把共享节点集合改名为决策树。
在 343 条 expanded test 上，五层子图 Top-1 历史标签一致率 58.02%，
低于完整特征维度的 65.89%。

需确认两项：节点/类型边 schema 是否符合专家对证据图的定义；报告已把图分数改为
typed edge 的 IDF-Jaccard，节点只用于解释，但 0.70 分区阈值仍需用人工确认路径重新标定。

未确认前的默认取值：**仅用于 HTML 描述性二维分区，不接入 N4；因果/SOP 路径统一标记为
`unavailable`，不从 token overlap 推断。**

### V24 监督学习连续谓词的稳定性门禁

| 项 | 内容 |
| --- | --- |
| 状态 | `待确认` |
| 责任人 | 陈 + 夏 |
| 阻塞 | 只阻塞将 learned predicate 接入正式 N3/N4；不阻塞离线 HTML 审计 |
| 提出于 | 五层证据图连续范围改造，2026-08-16 |

当前默认只在清洗并应用专家裁决后的 122 train 上做一维监督切分，要求：观测数至少 40、完整训练叶支持至少 10、
Gini gain 至少 0.03、5-fold 阈值极差 / 全量值域不超过 0.15，且相邻区间的多数标签不同。
16 个候选中只有 `L2.media_snr_min <= 23.805` 与 `L2.rxpower lane spread > 2.165`
通过；只有相对训练基线标签分布偏移更大的特殊分支进入图，普通分支不建症状路径。

需确认：门禁参数是否作为论文/正式实验口径，以及是否需要用多 seed、bootstrap 或新人工确认
case 替代当前 5-fold 稳定性。未确认前的默认取值：**模型与完整候选审计写入
`learned_predicate_model.json`，只用于离线 HTML；不写入物理约束库、不修改 N4、不回灌 N8。**

### V26 专家说明中的绝对 TX-RX 损耗逻辑

| 项 | 内容 |
| --- | --- |
| 状态 | `待确认` |
| 责任人 | 夏 + 黄 + 王 |
| 阻塞 | 是否能把 6 个专家 pair 的损耗说明沉淀为正式物理关系 |
| 提出于 | Expanded expert-clean v1，2026-08-17 |

44 个专家 pair 中有 6 个备注直接使用 `TX-RX` 或“两端光衰”解释标签。但 V7/C12 已证明当前
数据的两端 lane 编号、标定口径和采集时刻不足以支持绝对损耗：训练数据甚至会得到物理上不可能
的负损耗。因此专家的标签裁决可以进入版本化数据契约，但这 6 条理由不能自动升级为物理约束。

未确认前的默认取值：输出到 `secondary_physics_review_pairs.json`，证据图只使用 side-level
TX 是否存在、对端 RX 是否触底等关系；禁止逐 lane 配对和绝对 TX-RX subtraction。

### V27 双维完全匹配与 SOP 自动终裁均未通过训练校准

| 项 | 内容 |
| --- | --- |
| 状态 | `待新增人工路径/样本后复核` |
| 责任人 | 陈 + 夏 |
| 阻塞 | N5a 覆盖率和确定性 SOP 自动终裁；不阻塞选择性补采/人工介入 |
| 提出于 | Expanded dual-similarity + executable SOP，2026-08-18 |

122 条 clean train 留一法中，没有任何双阈值组合同时满足支持至少 20、选择性精度至少 85%；
按既定策略退回双维均为 1.0。该严格门禁在 341 test 仅覆盖 1 条，且在 42 条专家审核子集中
该条为错误标签复用，因此目前不能把“结构和 token 完全相同”解释成已确认因果路径完全相同。
此外，当前确定性 SOP 在 train 上 42 个唯一端点输出仅 22 个正确（52.38%），自动终裁已关闭。

未确认前默认：N5a 仍按用户确认的双维 1.0 规则作为消融展示，但生产必须报告其校准支持为 0；
SOP 只生成候选、排查步骤和补采项。LLM 只有在远端实验的 42 条专家审核 N5b/N5c 上相对 SOP
产生正向净增益且不降低选择性精度，下一版本才允许诊断干预，否则仅作解释/补采生成器。

### V28 完整 pytest 与 DeepSeek-32B 双维正式实验待远端执行

| 项 | 内容 |
| --- | --- |
| 状态 | `待远端运行` |
| 责任人 | 陈 |
| 阻塞 | 正式实验数字与 LLM 权限结论 |
| 提出于 | Expanded dual-similarity + executable SOP，2026-08-18 |

当前本机 Python 未安装 pytest；已完成编译、shell 语法、7 个新增定向测试和 legacy 58/85 手工回归，
但完整 `python -m pytest -q` 必须在服务器环境补跑。DeepSeek-32B 四组消融也必须通过
`run_synced_expanded_experiment.sh` 生成 GPU 前后快照、逐轮 LLM trace、summary 和 HTML 后才能形成结论。
未执行前默认：LLM 权限为 `explanation_and_evidence_request_only`，不得宣称提升诊断效果。

---

## 6. 汇总

| 编号 | 事项 | 类型 | 责任人 | 状态 | 阻塞 |
| --- | --- | --- | --- | --- | --- |
| V1 | N4 分流阈值重新标定 | 决策 | 陈 + 夏 | 待确认（已有建议） | T5 |
| V2 | 是否接受 N5a 变小 | 决策 | 夏 | 待确认 | 论文口径 |
| V3 | fiber 的目标指标 | 决策 | 夏 | 待确认 | T10 / T12 |
| V4 | 约束库 14 条逐条审核 | 专家 | 夏 | 待确认 | T6 |
| V5 | L2 侧温度偏高 3.5 °C 的原因 | 专家 | 夏 | 待确认 | 不阻塞 |
| V6 | 单例 3.10 V 低电压是否真实 | 专家 | 夏 + 黄 | 待确认 | 不阻塞 |
| V7 | 两端 lane 编号是否物理对应 | 厂商 | 黄 + 王 | 待确认 | 方向性判断 |
| V8 | `serdes_snr` 量纲 | 厂商 | 黄 + 王 | 待确认 | 可解释性 |
| V9 | `host_snr` 缺失原因 | 数据 | 黄 | 待确认 | N6 充分性 |
| V10 | 合并数据集交付与 fiber 样本数 | 数据 | 黄 + 王 | 待确认 | T5 / T10 |
| V11 | 专家排障 SOP 是否按期 | 数据 | 黄 + 谢 | 待确认 | T6（有兜底） |
| V12 | 告警端口恒为 L2 侧 | 数据 | 黄 | 待确认 | 不阻塞 |
| V13 | 告警类型只有 3 种 | 数据 | 黄 | 待确认 | 不阻塞 |
| V16 | 全链路遥测失效的 6 条该不该回答 | 决策 + 数据 | 夏 + 黄 | 已确认（expert-clean v1 剔除） | 不阻塞 |
| V17 | T6 真实模型首测 | 资源 / 实验 | 陈 + 黄 | 已确认（工程首测完成） | 正式实验门禁仍待补 |
| V18 | M9 Wilson 下界与最小支持数 | 决策 | 陈 + 夏 | 待确认（可配置） | T10 / 论文工作点 |
| V19 | l2fixed learned SOP 论文口径与风险 | 决策 | 陈 + 夏 | 已确认 | 论文表述 |
| V20 | 非自进化正式实验采用 SOP+LLM 与训练知识包边界 | 决策 | 陈 | 已确认 | 正式实验门禁 |
| V14 | N5a 多数投票口径修正 | 口径 | 陈 | 已确认 | — |
| V15 | legacy 匹配准确率统计范围 | 口径 | 陈 | 已确认 | — |
| V21 | 专家决策树 fiber 分支条件是否放宽 | 决策 | 陈 + 夏 | 待确认（现按文档原样保留，231 例 0 次触发） | fiber 指标 |
| V22 | `all_data` 是否为严格扩充版、6 条旧 case 去向 | 数据契约 | 黄 + 王 + 陈 | 待确认（默认按内容差 258 条新增） | 正式新增样本口径 |
| V23 | 可观测证据子图 schema 与阈值 | 证据图 / 决策 | 陈 + 夏 | 待确认（当前仅 HTML 描述性审计） | 正式 N4 二维路由 |
| V24 | 监督学习连续谓词稳定性门禁 | 证据图 / 统计 | 陈 + 夏 | 待确认（当前仅离线模型） | 正式 N3/N4 learned predicate |
| V25 | 远端模型路径与 GPU 拓扑 | 资源 / 实验 | 陈 | 已确认（DeepSeek-Qwen-32B、TP=2） | — |
| V26 | 专家说明中的绝对 TX-RX 损耗逻辑 | 专家 + 厂商 | 夏 + 黄 + 王 | 待确认（默认禁止进入约束） | 物理关系沉淀 |
| V27 | 双维完全匹配与 SOP 自动终裁均未通过训练校准 | 方法 + 数据 | 陈 + 夏 | 待新增人工路径/样本后复核 | N5a 覆盖 / SOP 自动终裁 |
| V28 | 完整 pytest 与 DeepSeek-32B 双维正式实验 | 工程 + 实验 | 陈 | 待远端运行 | 正式数字 / LLM 权限 |

真正阻塞后续完整消融的是 V10（合并数据）；V1 已有双策略实现，不再阻塞编码；
V17 的 GPU 与模型资源问题已关闭，正式实验记录已由 V20/Progress 9.30 补齐；
真机效果仍未达标。
V1 在 T4 之后已经从「不知道怎么定」变成「有明确建议等拍板」，且 T5 会把两套规则都实现，
因此它实际上也不再阻塞编码，只阻塞画板与论文的表述定稿。

V7、V8 与 V16 虽然不阻塞代码，但它们决定了现有部分「可解释规则」到底解释了什么，
优先级应当高于其它数据类事项。其中 V16 的后半段问题——
链路整体中断时遥测是否会把所有光学读数写成哨兵默认值——影响面最大，
如果答案是「会」，那么数据集里全部的「断光」证据都需要重新审视。
